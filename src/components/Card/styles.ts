import { StyleSheet } from "react-native";

export const styles=StyleSheet.create({
    card:{
        marginTop:10,
        height:60,
        backgroundColor:"#333333",
        marginHorizontal:16,
        borderRadius:8,
        flexDirection:'row',
        alignItems:'center'
    }
})